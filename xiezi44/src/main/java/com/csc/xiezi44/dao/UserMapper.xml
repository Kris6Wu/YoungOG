<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.csc.xiezi44.dao.UserDao">
<cache></cache>
 <!--  <update id="update">
   update user 
   <set>
	   	<if test="uname!=null">
	   		uname=#{uname},
	   	</if>
	   	<if test="upass!=null">
	   		upass=#{upass},
	   	</if>
	   	<if test="sex!=null">
	   		sex=#{sex},
	   	</if>
	   	<if test="tel!=null">
	   		tel=#{tel},
	   	</if> 
   </set> 
   
   where uname=#{name} 
  </update> -->
 
 
 
</mapper>
